# Backlog — Novify API

Le funzionalità che implementeremo nei prossimi moduli (prima come mock, poi come backend reale).
Per questo esercizio **non scrivi codice**: prenoti un task scrivendo il tuo nome tra parentesi sulla riga, poi apri la Pull Request. L'obiettivo è provare commit, PR e conflitti — non l'implementazione.

(Questo file serve all'esercizio: tutti modificano lo stesso file → così proviamo commit, PR e conflitti.)

- [ ] `GET /api/v1/tracks` — lista dei brani ( )
- [ ] `GET /api/v1/tracks/{id}` — dettaglio di un brano ( )
- [ ] `POST /api/v1/tracks` — crea un nuovo brano ( )
- [ ] `GET /api/v1/playlists` — lista delle playlist ( )
- [ ] `GET /api/v1/playlists/{id}` — dettaglio di una playlist ( )
- [ ] `POST /api/v1/playlists` — crea una nuova playlist ( )
- [ ] `PUT /api/v1/playlists/{id}` — aggiungi un brano a una playlist ( )
- [ ] `GET /api/v1/tracks?genere=...` — ricerca brani per genere ( )
